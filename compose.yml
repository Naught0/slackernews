services:
  app:
    image: slackernews:latest
    build: .
    restart: unless-stopped
  nginx:
    restart: unless-stopped
    image: nginx
    ports:
      - "127.0.0.1:3006:80"
    volumes:
      - ./nginx/:/etc/nginx/
    depends_on:
      - app
