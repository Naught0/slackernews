services:
  app:
    image: slackernews:latest
    build: .
    restart: always
  nginx:
    image: nginx
    ports:
      - "127.0.0.1:3006:80"
    volumes:
      - ./nginx/:/etc/nginx/
    depends_on:
      - app
